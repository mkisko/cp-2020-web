<form method="post" action="{{ path('user_education_delete', {'id': user_education.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_education.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
