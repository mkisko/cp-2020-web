{% extends 'admin/layout.html.twig' %}

{% block title %}UserEducation{% endblock %}

{% block body %}
    <h1>UserEducation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_education.id }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ user_education.startDate ? user_education.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ user_education.endDate ? user_education.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_education_index') }}">back to list</a>

    <a href="{{ path('user_education_edit', {'id': user_education.id}) }}">edit</a>

    {{ include('admin/user_education/_delete_form.html.twig') }}
{% endblock %}
