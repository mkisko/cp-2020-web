{% extends 'base.html.twig' %}

{% block title %}UserStageProgress{% endblock %}

{% block body %}
    <h1>UserStageProgress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_stage_progress.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ user_stage_progress.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_stage_progress_index') }}">back to list</a>

    <a href="{{ path('user_stage_progress_edit', {'id': user_stage_progress.id}) }}">edit</a>

    {{ include('user_stage_progress/_delete_form.html.twig') }}
{% endblock %}
