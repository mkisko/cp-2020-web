{% extends 'hirer/layout.html.twig' %}

{% block title %}Edit Company{% endblock %}

{% block subheader %}
    <div class="subheader py-2 py-lg-6  subheader-transparent " id="kt_subheader">
        <div class="container d-flex align-items-center justify-content-between flex-wrap flex-sm-nowrap">
            <div class="d-flex align-items-center flex-wrap mr-1">
                <button class="burger-icon burger-icon-left mr-4 d-inline-block d-lg-none" id="kt_subheader_mobile_toggle">
                    <span></span>
                </button>

                <div class="d-flex align-items-baseline flex-wrap mr-5">
                    <h5 class="text-dark font-weight-bold my-1 mr-5">Изменение компании</h5>

                    <ul class="breadcrumb breadcrumb-transparent breadcrumb-dot font-weight-bold p-0 my-2 font-size-sm">
                        <li class="breadcrumb-item">
                            <a class="text-muted">Главная</a>
                        </li>
                        <li class="breadcrumb-item">
                            <a class="text-muted">Работодатель</a>
                        </li>
                        <li class="breadcrumb-item">
                            <a class="text-muted">Изменение компании</a>
                        </li>
                    </ul>
                </div>
            </div>

            <div class="d-flex align-items-right">
                Удаление не возможно
            </div>
        </div>
    </div>
{% endblock %}

{% block body %}
    <div class="container">
        <div class="card gutter-b">
            <div class="card-body">
                {{ include('admin/vacancy/_form.html.twig') }}
            </div>
        </div>

        <a href="{{ path('hirer_show_profile', {'id': company.id}) }}">Назад к просмотру компании</a>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        $('form[name=company]').each(function() {
            $(this).find(':input').each(function () {
                if ($(this)[0].localName == 'button') return

                $(this).addClass('form-control')
            })
        })
    </script>
{% endblock %}